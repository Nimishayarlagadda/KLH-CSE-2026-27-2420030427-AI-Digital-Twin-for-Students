# Test Plan

Unit, API, integration, ML evaluation, system and user acceptance testing will be used.

Core flow:
Student input -> database -> Digital Twin -> prediction -> risk -> recommendation -> dashboard.
